version: '3.8'
services:
  app:
    build: .
    container_name: pki-2fa-app
    ports:
      - "8080:8080"
    volumes:
      - seed-data:/data
      - cron-output:/cron
    environment:
      - TZ=UTC
volumes:
  seed-data:
  cron-output:
